allowBuilds:
  argon2: true
  esbuild: true
  sharp: true
  unrs-resolver: true
overrides:
  # browserslist data package that publishes daily; pin an older, widely
  # distributed version so pnpm's minimumReleaseAge policy stays satisfied.
  electron-to-chromium: 1.5.394
