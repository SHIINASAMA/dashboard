# Dashboard Knowledge Base

## Rules (MUST follow)

- **NEVER commit or `git push` without explicit user permission.** This is the most important rule. The user must explicitly ask you to commit or push before you do so. Running `/goal` does not count as permission to commit.
- **NEVER delete `data/dashboard.db`** — use migrations for schema changes
